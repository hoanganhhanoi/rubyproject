<% provide(:title, @user.name) %>

<p id="notice"><%= notice %></p>

<h1> 
  <%= gravatar_for @user %>
  <%= p @user.name %>
</h1>
<p>
  <strong>Name:</strong>
  <%= p @user.name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
